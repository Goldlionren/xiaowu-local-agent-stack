# Plugins

The deployment uses separate plugins for memory injection, model routing, and visual execution. Production plugin source is excluded because no local license file was found.

Keep allow_tool_override false for routing and memory policy plugins. Grant the visual plugin only the MCP actions required by its transaction controller.
