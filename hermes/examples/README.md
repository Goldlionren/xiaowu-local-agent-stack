# Hermes examples

Use config/hermes.config.example.yaml as a minimal shape. Merge settings deliberately into your own Hermes version; configuration schemas can change.

Keep secrets in a private HERMES_HOME environment file and host-specific MCP arguments outside Git.
