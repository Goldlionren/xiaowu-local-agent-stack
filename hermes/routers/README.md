# Routers

The active route maps xiaowu-avatar to the local yinyue2 model through provider yinyue2-local. The legacy /yinyue_avatar control command remains a compatibility alias.

Route state should live under the Xiaowu namespace. Treat profile mirrors as deployable copies that require hash comparison before restart.
