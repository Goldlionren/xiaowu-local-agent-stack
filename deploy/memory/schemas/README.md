# Schemas

Production schema and migration source were not copied because the local custom service had no confirmed license.

The audited control schema contained:

- ingest_ledger
- source_event_registry
- turn_candidate_manifest
- turn_registry

Use the upstream Hindsight migrations for Hindsight-owned tables and publish custom migrations only after assigning a license and reviewing them for embedded data.
