# Docker

The audited Docker Engine was 29.4.1 and Compose was 5.1.3. Docker hosted Hindsight and PostgreSQL; Hermes and llama.cpp ran as host services.

See deploy/memory/docker-compose.example.yml. Do not use prune or destructive volume commands as part of health checks.
