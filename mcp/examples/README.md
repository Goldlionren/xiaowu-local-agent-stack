# MCP examples

Use SSH BatchMode with a narrowly scoped private identity and a remote command that starts the MCP stdio bridge. Mark workflow submission as non-replayable. Keep raw toolsets disabled for ordinary model turns and expose a policy-controlled business tool.

Connection tests should discover tools without invoking workflow, launch, stop, update, download, or other mutating operations.
