# Installation

No unattended installer is supplied. The audited stack contains host-specific drivers, GPU selection, model files, system units, private MCP transports, and persistent data. Apply each example through an explicit change window and verify it before enabling services.
