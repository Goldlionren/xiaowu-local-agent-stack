# Comfy MCP As-Built

## Avatar path

~~~mermaid
flowchart LR
    H[Hermes] --> T[xiaowu_avatar_generate]
    T --> P[xiaowu-visual]
    P --> X[MCP stdio over SSH]
    X --> C[comfy_3060]
    C --> W[XiaowuAvatar workflow]
    W --> O[generated output]
    O --> F[fetch and SHA/state commit]
    F --> D[configured delivery]
~~~

The active avatar target is comfy_3060. A live MCP test connected and discovered 39 tools. The remote project root is parameterized as REMOTE_XIAOWU_ROOT in public examples; the audited Windows root ended in XiaowuAvatar.

comfy_5090 is DISABLED BY DESIGN in the avatar workflow registry. Its workflow was marked absent and its contract unverified. A direct connection test also failed, and the gateway parked repeated connection attempts. This is expected for avatar routing, though disabling the lower-level Hermes MCP entry would reduce log noise in a future change window.

comfy_4080s connected and exposed 39 tools but is not the active Xiaowu avatar target. The ComfyTV HTTP MCP endpoint timed out during the audit.

The latest revision 20 transaction proves workflow generation, output retrieval, local commit, and configured delivery. No new image was generated by this audit.
